apiVersion: v1
kind: Service
metadata:
  name: calculater-service
  namespace: jenkin

spec:
  selector:
    app: calculater

  ports:
    - name: http
      protocol: TCP
      port: 8084
      targetPort: 8084

  type: NodePort
